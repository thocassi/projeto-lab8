/**
 * Runtime helpers used by generated schema modules. This module contains no
 * source generator or dynamic function construction. Generated modules must
 * use the same Effect version as their generator.
 *
 * @since 4.0.0
 */
import * as Effect from "../../../Effect.js";
import { effectIsExit, resolveConcurrency } from "../../../internal/effect.js";
import { lazyParser, resolve, setCompiler, withDecode } from "../../../internal/schema/compilerRegistry.js";
import * as Interpreter from "../../../internal/schema/interpreter.js";
import * as InternalParser from "../../../internal/schema/parser.js";
import * as SchemaAST from "../../../SchemaAST.js";
import * as SchemaIssue from "../../../SchemaIssue.js";
import { invalid } from "../SchemaCompiler.js";
const makeObjectBase = (ast, compile, compileField, generate) => {
  let properties;
  const getProperties = () => {
    if (properties !== undefined) return properties;
    const parsers = new Map();
    return properties = ast.propertySignatures.map(property => {
      let parser = parsers.get(property.type);
      if (parser === undefined) {
        parser = compileField(property.type);
        parsers.set(property.type, parser);
      }
      return {
        parser,
        name: property.name,
        type: property.type
      };
    });
  };
  let fallback;
  const runFallback = (input, options) => (fallback ??= ast.getParser(compile, compileField))(input, options);
  const resume = (state, index, pending) => {
    const property = properties[index];
    return Effect.flatMap(Effect.exit(pending), exit => {
      const terminal = SchemaAST.stepProperty(state, property, exit);
      if (terminal) return terminal;
      const done = () => InternalParser.succeed(state.out);
      const effect = SchemaAST.parseProperties(state, properties.slice(index + 1));
      return effect ? Effect.flatMapEager(effect, done) : done();
    });
  };
  return generate({
    ast,
    getProperties,
    fallback: runFallback,
    resume,
    step: SchemaAST.stepProperty
  });
};
const makeArrayBase = (ast, compile, compileField, generate) => {
  let element;
  const getElement = () => element ??= {
    ast: ast.rest[0],
    parser: compileField(ast.rest[0])
  };
  let fallback;
  const runFallback = (input, options) => (fallback ??= ast.getParser(compile, compileField))(input, options);
  const run = generate({
    getElement: () => getElement().parser,
    step: SchemaAST.stepArray,
    resume: (state, item, index, pending, end) => Effect.flatMap(Effect.exit(pending), exit => SchemaAST.stepArray(state, item, exit, index) ?? SchemaAST.parseArray(state, state.input, index + 1, end) ?? Effect.void)
  });
  const specialized = Effect.fnUntracedEager(function* (input, options) {
    if (input === InternalParser.missing) return InternalParser.missing;
    if (!Array.isArray(input)) {
      return yield* Effect.fail(new SchemaIssue.InvalidType(ast, input, options));
    }
    const descriptor = getElement();
    const len = input.length;
    const state = {
      ast,
      getParser: () => descriptor,
      input,
      len,
      tailThreshold: len,
      output: new globalThis.Array(len),
      issues: undefined,
      options
    };
    const effect = run(state, input, 0, len);
    if (effect) yield* effect;
    if (state.issues) {
      return yield* Effect.fail(new SchemaIssue.Composite(ast, state.issues, input, options));
    }
    return state.output;
  });
  return (input, options) => options.concurrency !== undefined && resolveConcurrency(options.concurrency) !== 1 ? runFallback(input, options) : specialized(input, options);
};
const decode = (ast, resolve, generate, detailed = false, makeDecode, generateArray) => {
  const child = ast => lazyParser(resolve, ast, detailed ? "decodeEffect" : "parser");
  const localChild = makeDecode === undefined ? child : ast => lazyParser(resolve, ast, "decodeEffect");
  const base = ast._tag === "Objects" && generate !== undefined ? makeObjectBase(ast, localChild, localChild, generate) : ast._tag === "Arrays" && generateArray !== undefined ? makeArrayBase(ast, localChild, localChild, generateArray) : makeDecode !== undefined ? ast.getParser(localChild) : undefined;
  const specialize = makeDecode === undefined ? undefined : local => {
    try {
      return withDecode(makeDecode(), () => local);
    } catch {
      // Initialization failure selects the local interpreter, without parsing again.
      return local;
    }
  };
  return Interpreter.compile(ast, child, undefined, base, specialize);
};
const make = (ast, resolve, generate, generateArray) => {
  const child = ast => lazyParser(resolve, ast, "makeEffect");
  const field = ast => Interpreter.compileField(ast, child);
  const base = generate !== undefined && ast._tag === "Objects" ? makeObjectBase(ast, child, field, generate) : generateArray !== undefined && ast._tag === "Arrays" ? makeArrayBase(ast, child, field, generateArray) : undefined;
  return Interpreter.compile(ast, child, field, base);
};
const getCheckIssues = (ast, value, encoded, options) => {
  const checks = encoded ? "encodingChecks" in ast ? ast.encodingChecks : undefined : ast.checks;
  return !options.disableChecks && checks !== undefined ? SchemaAST.collectIssues(checks, value, undefined, ast, options) : undefined;
};
const check = (ast, value, options) => {
  const issues = getCheckIssues(ast, value, false, options);
  return issues === undefined ? InternalParser.succeed(value) : Effect.fail(new SchemaIssue.Composite(ast, issues, value, options));
};
const hasExcessProperties = (ast, input, options) => {
  const covered = new Set(ast.propertySignatures.map(p => typeof p.name === "number" ? String(p.name) : p.name));
  for (const index of ast.indexSignatures) {
    for (const key of SchemaAST.getIndexSignatureKeys(input, index.parameter, options)) covered.add(key);
  }
  return Reflect.ownKeys(input).some(key => !covered.has(key));
};
const invalidType = (ast, input, options) => Effect.fail(new SchemaIssue.InvalidType(ast, input, options));
const invalidEncoding = (ast, index, input, value, options) => index === 0 ? invalidType(ast, value, options) : Interpreter.wrapEncoding(ast, input, options, invalidType(ast.encoding[index - 1].to, value, options));
/** @internal */
export const runtime = {
  decode,
  make,
  resolve,
  setCompiler,
  invalid,
  missing: InternalParser.missing,
  missingExit: InternalParser.missingExit,
  sameExit: InternalParser.sameExit,
  args: InternalParser.args,
  succeed: InternalParser.succeed,
  effectIsExit,
  die: Effect.die,
  invalidType,
  invalidEncoding,
  getCheckIssues,
  check,
  getExpectedKeys: ast => ast.propertySignatures.map(p => typeof p.name === "number" ? String(p.name) : p.name),
  hasExcessProperties,
  matchesTemplateLiteral: (ast, input, options) => typeof input === "string" && ast.matchPart(input, options) !== undefined,
  getCandidates: SchemaAST.getCandidates,
  getIndexSignatureKeys: SchemaAST.getIndexSignatureKeys,
  parameterFromPropertyKey: SchemaAST.parameterFromPropertyKey,
  getConstructorDescriptor: SchemaAST.getConstructorDescriptor,
  defaultParseOptions: SchemaAST.defaultParseOptions
};
//# sourceMappingURL=runtime.js.map